// src/screens/home/components/extract/styles.ts
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  filterCount: {
    fontSize: 16,
    fontWeight: "bold",
    color: "#FFF",
    marginRight: 8, // Espaço entre o contador e o botão de filtro
  },
});
